# Local Terraform state (never commit — state lives in Azure Storage)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform plan files
*.tfplan
tfplan

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Crash log files
crash.log
crash.*.log

# Override files (local developer overrides — not for CI)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
